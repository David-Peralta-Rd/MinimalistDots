diff --git a/src/lang/en.cfg b/src/lang/en.cfg
index f9156e7..e03e12c 100644
--- a/src/lang/en.cfg
+++ b/src/lang/en.cfg
@@ -59,3 +59,11 @@ TXT_KEYBINDS_OK="Keyboard shortcuts configured and registered successfully."
 # --- Hyprland Root Files ---
 TXT_HYPR_ROOT_INSTALLING="Configuring main files in ~/.config/hypr/..."
 TXT_HYPR_ROOT_OK="hyprland.lua, hypridle, hyprlock, and hyprpaper files generated successfully."
+
+# --- Package and Module Installation (install_hypr.sh / install_complex.sh) ---
+TXT_NO_PACKAGES="No packages to install."
+TXT_INSTALLING_PACKAGES="Installing packages:"
+TXT_PACKAGES_DONE="Packages installed successfully."
+TXT_COMPLEX_RUNNING="Running module:"
+TXT_ENABLING_SDDM="Enabling session manager (SDDM)..."
+TXT_RELOADING="Reloading Hyprland configuration..."
diff --git a/src/lang/es.cfg b/src/lang/es.cfg
index 25615c7..b80e615 100644
--- a/src/lang/es.cfg
+++ b/src/lang/es.cfg
@@ -59,3 +59,11 @@ TXT_KEYBINDS_OK="Atajos de teclado configurados y registrados exitosamente."
 # --- Archivos Base Hyprland ---
 TXT_HYPR_ROOT_INSTALLING="Configurando archivos principales en ~/.config/hypr/..."
 TXT_HYPR_ROOT_OK="Archivos hyprland.lua, hypridle, hyprlock y hyprpaper generados correctamente."
+
+# --- Instalación de Paquetes y Módulos (install_hypr.sh / install_complex.sh) ---
+TXT_NO_PACKAGES="No hay paquetes para instalar."
+TXT_INSTALLING_PACKAGES="Instalando paquetes:"
+TXT_PACKAGES_DONE="Paquetes instalados correctamente."
+TXT_COMPLEX_RUNNING="Ejecutando módulo:"
+TXT_ENABLING_SDDM="Habilitando el gestor de sesión (SDDM)..."
+TXT_RELOADING="Recargando configuración de Hyprland..."
diff --git a/src/scripts/install_complex.sh b/src/scripts/install_complex.sh
index 5af88e4..ea0b515 100755
--- a/src/scripts/install_complex.sh
+++ b/src/scripts/install_complex.sh
@@ -14,18 +14,10 @@ else
     exit 1
 fi
 
-# 1. Instalación de paquetes genéricos declarados en packages.sh
-source "$SCRIPT_DIR/packages.sh"
+# Nota: los paquetes de packages.sh ya se instalan en install_hypr.sh,
+# por eso aquí solo se ejecutan los módulos de complex_install/ (no se repite paru).
 
-if [ ${#ALL_PKGS[@]} -gt 0 ]; then
-    echo "=========================================================="
-    echo "$TXT_INSTALLING_PACKAGES ${#ALL_PKGS[@]}"
-    echo "=========================================================="
-    paru -S --needed --noconfirm "${ALL_PKGS[@]}"
-    echo "$TXT_PACKAGES_DONE"
-fi
-
-# 2. Ejecutar instalaciones complejas / módulos personalizados
+# 1. Ejecutar instalaciones complejas / módulos personalizados
 COMPLEX_DIR="$SCRIPT_DIR/complex_install"
 if [ -d "$COMPLEX_DIR" ]; then
     shopt -s nullglob
@@ -35,7 +27,7 @@ if [ -d "$COMPLEX_DIR" ]; then
     for script in "${COMPLEX_SCRIPTS[@]}"; do
         NAME="$(basename "$script" .sh)"
         echo "=========================================================="
-        echo "$TXT_COMPLEX_RUNNING $NAME"
+        echo "${TXT_COMPLEX_RUNNING:-Ejecutando módulo:} $NAME"
         echo "=========================================================="
         bash "$script" "$ARCHIVO_LANG"
     done
diff --git a/src/scripts/install_hypr.sh b/src/scripts/install_hypr.sh
index 8d21164..2296b75 100755
--- a/src/scripts/install_hypr.sh
+++ b/src/scripts/install_hypr.sh
@@ -18,15 +18,15 @@ fi
 source "$SCRIPT_DIR/packages.sh"
 
 if [ ${#ALL_PKGS[@]} -eq 0 ]; then
-    echo "$TXT_NO_PACKAGES"
+    echo "${TXT_NO_PACKAGES:-No hay paquetes para instalar.}"
     exit 0
 fi
 
 echo "=========================================================="
-echo "$TXT_INSTALLING_PACKAGES ${#ALL_PKGS[@]}"
+echo "${TXT_INSTALLING_PACKAGES:-Instalando paquetes:} ${#ALL_PKGS[@]}"
 echo "=========================================================="
 paru -S --needed --noconfirm "${ALL_PKGS[@]}"
-echo "$TXT_PACKAGES_DONE"
+echo "${TXT_PACKAGES_DONE:-Paquetes instalados correctamente.}"
 
 # 2. Instalaciones complejas / Módulos de Hyprland
 INSTALL_HYPR="$SCRIPT_DIR/installs_hypr"
@@ -38,18 +38,18 @@ if [ -d "$INSTALL_HYPR" ]; then
     for script in "${COMPLEX_SCRIPTS[@]}"; do
         NAME="$(basename "$script" .sh)"
         echo "=========================================================="
-        echo "$TXT_COMPLEX_RUNNING $NAME"
+        echo "${TXT_COMPLEX_RUNNING:-Ejecutando módulo:} $NAME"
         echo "=========================================================="
         bash "$script" "$ARCHIVO_LANG"
     done
 fi
 
 # 3. Habilitar el gestor de sesión (SDDM)
-echo "$TXT_ENABLING_SDDM"
+echo "${TXT_ENABLING_SDDM:-Habilitando el gestor de sesión (SDDM)...}"
 sudo systemctl enable sddm
 
 # 4. Recarga de Hyprland si la sesión está en ejecución
 if [ -n "${HYPRLAND_INSTANCE_SIGNATURE:-}" ]; then
-    echo "$TXT_RELOADING"
+    echo "${TXT_RELOADING:-Recargando configuración de Hyprland...}"
     hyprctl reload
 fi
diff --git a/src/scripts/installs_hypr/setup_colors.sh b/src/scripts/installs_hypr/setup_colors.sh
index 60bf0a6..b9e0dd6 100755
--- a/src/scripts/installs_hypr/setup_colors.sh
+++ b/src/scripts/installs_hypr/setup_colors.sh
@@ -3,7 +3,7 @@ set -euo pipefail
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
 LANG_FILE="${1:-es.cfg}"
-PATH_TRADUCCION="$SCRIPT_DIR/src/lang/$LANG_FILE"
+PATH_TRADUCCION="$SCRIPT_DIR/lang/$LANG_FILE"
 
 if [[ -f "$PATH_TRADUCCION" ]]; then
     source "$PATH_TRADUCCION"
diff --git a/src/scripts/installs_hypr/setup_hypr_root.sh b/src/scripts/installs_hypr/setup_hypr_root.sh
index 63d18d6..8ac7c86 100755
--- a/src/scripts/installs_hypr/setup_hypr_root.sh
+++ b/src/scripts/installs_hypr/setup_hypr_root.sh
@@ -3,7 +3,7 @@ set -euo pipefail
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
 LANG_FILE="${1:-es.cfg}"
-PATH_TRADUCCION="$SCRIPT_DIR/src/lang/$LANG_FILE"
+PATH_TRADUCCION="$SCRIPT_DIR/lang/$LANG_FILE"
 
 if [[ -f "$PATH_TRADUCCION" ]]; then
     source "$PATH_TRADUCCION"
diff --git a/src/scripts/installs_hypr/setup_keybinds.sh b/src/scripts/installs_hypr/setup_keybinds.sh
index 7a88d33..8d52f68 100755
--- a/src/scripts/installs_hypr/setup_keybinds.sh
+++ b/src/scripts/installs_hypr/setup_keybinds.sh
@@ -3,7 +3,7 @@ set -euo pipefail
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
 LANG_FILE="${1:-es.cfg}"
-PATH_TRADUCCION="$SCRIPT_DIR/src/lang/$LANG_FILE"
+PATH_TRADUCCION="$SCRIPT_DIR/lang/$LANG_FILE"
 
 if [[ -f "$PATH_TRADUCCION" ]]; then
     source "$PATH_TRADUCCION"
diff --git a/src/scripts/installs_hypr/setup_lib.sh b/src/scripts/installs_hypr/setup_lib.sh
index 61e733a..1f3a12f 100755
--- a/src/scripts/installs_hypr/setup_lib.sh
+++ b/src/scripts/installs_hypr/setup_lib.sh
@@ -4,7 +4,7 @@ set -euo pipefail
 # 1. Cargamos idioma y rutas
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
 LANG_FILE="${1:-es.cfg}"
-PATH_TRADUCCION="$SCRIPT_DIR/src/lang/$LANG_FILE"
+PATH_TRADUCCION="$SCRIPT_DIR/lang/$LANG_FILE"
 
 if [[ -f "$PATH_TRADUCCION" ]]; then
     source "$PATH_TRADUCCION"
diff --git a/src/scripts/installs_hypr/setup_services.sh b/src/scripts/installs_hypr/setup_services.sh
index 4e0dcc0..553caf6 100755
--- a/src/scripts/installs_hypr/setup_services.sh
+++ b/src/scripts/installs_hypr/setup_services.sh
@@ -3,7 +3,7 @@ set -euo pipefail
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
 LANG_FILE="${1:-es.cfg}"
-PATH_TRADUCCION="$SCRIPT_DIR/src/lang/$LANG_FILE"
+PATH_TRADUCCION="$SCRIPT_DIR/lang/$LANG_FILE"
 
 if [[ -f "$PATH_TRADUCCION" ]]; then
     source "$PATH_TRADUCCION"
diff --git a/src/scripts/installs_hypr/setup_show_binds.sh b/src/scripts/installs_hypr/setup_show_binds.sh
index 0c433e8..44cbf0d 100755
--- a/src/scripts/installs_hypr/setup_show_binds.sh
+++ b/src/scripts/installs_hypr/setup_show_binds.sh
@@ -4,7 +4,7 @@ set -euo pipefail
 # 1. Rutas y carga de idiomas
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
 LANG_FILE="${1:-es.cfg}"
-PATH_TRADUCCION="$SCRIPT_DIR/src/lang/$LANG_FILE"
+PATH_TRADUCCION="$SCRIPT_DIR/lang/$LANG_FILE"
 
 if [[ -f "$PATH_TRADUCCION" ]]; then
     source "$PATH_TRADUCCION"
diff --git a/src/scripts/installs_hypr/setup_vars.sh b/src/scripts/installs_hypr/setup_vars.sh
index 2a36047..8235b05 100755
--- a/src/scripts/installs_hypr/setup_vars.sh
+++ b/src/scripts/installs_hypr/setup_vars.sh
@@ -4,7 +4,7 @@ set -euo pipefail
 # 1. Rutas y carga de idioma
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
 LANG_FILE="${1:-es.cfg}"
-PATH_TRADUCCION="$SCRIPT_DIR/src/lang/$LANG_FILE"
+PATH_TRADUCCION="$SCRIPT_DIR/lang/$LANG_FILE"
 
 if [[ -f "$PATH_TRADUCCION" ]]; then
     source "$PATH_TRADUCCION"
diff --git a/src/scripts/installs_hypr/setup_wofi.sh b/src/scripts/installs_hypr/setup_wofi.sh
index 2a2573f..9162e5d 100755
--- a/src/scripts/installs_hypr/setup_wofi.sh
+++ b/src/scripts/installs_hypr/setup_wofi.sh
@@ -4,14 +4,14 @@ set -euo pipefail
 # 1. Definición de rutas y carga de idioma
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
 LANG_FILE="${1:-es.cfg}"
-PATH_TRADUCCION="$SCRIPT_DIR/src/lang/$LANG_FILE"
+PATH_TRADUCCION="$SCRIPT_DIR/lang/$LANG_FILE"
 
 if [[ -f "$PATH_TRADUCCION" ]]; then
     source "$PATH_TRADUCCION"
 fi
 
-# 2. Cargar paleta global de colores
-COLORS_FILE="$SCRIPT_DIR/src/scripts/colors.sh"
+# 2. Cargar paleta global de colores (archivo generado por colors.sh, no el script generador)
+COLORS_FILE="$SCRIPT_DIR/colors/palette.sh"
 if [[ -f "$COLORS_FILE" ]]; then
     source "$COLORS_FILE"
 else
diff --git a/src/scripts/installs_hypr/setup_wofi_cliphist.sh b/src/scripts/installs_hypr/setup_wofi_cliphist.sh
index 9f4d781..8b8ebb5 100755
--- a/src/scripts/installs_hypr/setup_wofi_cliphist.sh
+++ b/src/scripts/installs_hypr/setup_wofi_cliphist.sh
@@ -4,14 +4,14 @@ set -euo pipefail
 # 1. Rutas y carga de idiomas
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
 LANG_FILE="${1:-es.cfg}"
-PATH_TRADUCCION="$SCRIPT_DIR/src/lang/$LANG_FILE"
+PATH_TRADUCCION="$SCRIPT_DIR/lang/$LANG_FILE"
 
 if [[ -f "$PATH_TRADUCCION" ]]; then
     source "$PATH_TRADUCCION"
 fi
 
-# 2. Cargar paleta global de colores
-COLORS_FILE="$SCRIPT_DIR/src/scripts/colors.sh"
+# 2. Cargar paleta global de colores (archivo generado por colors.sh, no el script generador)
+COLORS_FILE="$SCRIPT_DIR/colors/palette.sh"
 if [[ -f "$COLORS_FILE" ]]; then
     source "$COLORS_FILE"
 else
